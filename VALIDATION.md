# 验证记录

2026-09-16，Apple Silicon 本机构建。

- Swift Release 编译通过。
- 应用包 Info.plist 格式校验通过。
- 干净临时目录中的应用通过 `codesign --verify --deep --strict` 校验。
- 从已验证应用生成 `dist/Walkie-Talkie.zip`，不包含文件扩展属性。
- `Scripts/test.sh` 的 26 项本地检查全部通过：URL 规范化、HTTPS / 本地 HTTP、凭据校验、输入与指令分离、响应解析、空响应、截断响应、HTTP 错误及编辑/清空状态。
- 已检查 1024px 和 256px 图标渲染，修正气泡尾部的路径方向。

尚未完成：

- 真实服务商联调：用户尚未提供 API 密钥；未调用付费 API。
- 真实窗口交互与全局快捷键自动验收：Computer Use 的辅助功能与屏幕录制授权未完成，未声称已经通过。
- 完整 XCTest 套件：当前独立 Command Line Tools 缺少 XCTest；已使用无第三方依赖的本地验证脚本替代。

应用自身的全局快捷键使用 Carbon 注册，不依赖 Computer Use 的辅助功能或屏幕录制权限。

## 1.1.0 更新验证

- 移除主窗口额外的 28pt 顶部补偿，改用系统标准标题栏内容区域；主视图顶部内边距由 22pt 减至 10pt。
- API 菜单最大宽度由 190pt 调至 160pt 并靠右对齐，与设置按钮的间距由 10pt 调至 8pt。
- 注册原生 NSServices「用对讲机翻译」，通过专用 Services pasteboard 接收选文；自动检测语言，沿用当前目标语言和 API。
- Release 编译、应用签名验证与 ZIP 内的版本/服务声明检查通过。
- 33 项本地检查通过；新增服务接收选文、保留段落、不改写来源内容、拒绝空白及非文本数据、兼容旧文本类型、清除错误、Objective-C 回调与 plist 声明匹配等检查。
- 尚未完成浏览器实际右键菜单及窗口视觉实测。安装至「应用程序」并启动后，系统才会刷新服务；菜单可见性还取决于浏览器对 macOS Services 的支持。

## 1.2.0 更新验证

- Swift Release 编译通过，版本号为 1.2.0，build 为 3。
- 50 项本地检查通过，新增凭据静默读取、授权后缓存、缓存替换/删除、旧钥匙串交互状态恢复、HTTPS 更新源验证及内置公钥格式验证。
- 应用及 Sparkle framework、Updater/Autoupdate、Downloader/Installer helper 的嵌套代码签名验证通过。ZIP 在干净临时目录构建后生成。
- Sparkle 已锁定到 2.10.0，确认主程序通过 @rpath 加载随包携带的 framework。
- 发布脚本完成 Python 语法检查。
- 更新签名专用密钥已生成于本机钥匙串，应用内只有公钥。
- generate_appcast 签名集成测试长时间无响应，疑似在等待钥匙串授权；已停止此次测试，未声称更新包 Ed25519 签名集成验证通过。后续用发布脚本可重新验证。
- 尚未提供真实发布地址，未发布 appcast 或安装包；未进行联网升级和安装重启实测。
- 无有效 Apple 代码签名身份，当前仍为本地 ad-hoc 构建。解决重复系统弹窗与避免跨版本重新授权是两件事；后者仍需稳定签名身份。

## 1.2.1 钥匙串修复验证

- 用户确认弹窗出现在翻译/划词翻译。检查发现两个本地副本均为 1.2.0，二进制 SHA-256 相同，未据此推断用户误用旧版。
- 新增持久化的密钥创建者签名标识；普通读取先校验标识。旧签名或未知签名在调用 Security 密钥读取 API 之前直接返回应用内提示，只有显式授权按钮可读取旧条目。
- 不自动读取或清理未知/旧签名条目；API 重填保存时创建新条目并记录当前签名。已有内存缓存继续可用。
- 54 项本地回归检查通过。
- 新增真实系统钥匙串集成测试：签名 A 创建唯一测试条目，A 重启后静默读取成功；签名 B 连续 10 次被拦截且真实钥匙串读取调用为零；A 再次读取成功；测试条目已删除。未读取用户 API 密钥。
- Release 构建和嵌套代码签名验证通过，版本为 1.2.1 / build 4；另提供明确版本号的 ZIP 防止拿错安装包。
- 没有声称在用户原有条目上直接复现并消除了操作系统弹窗；本次通过“读取前拒绝访问旧条目”移除了触发路径，而不再仅依赖系统禁止交互标志。
- 从旧配置升级需重新填写密钥并保存一次，或主动授权读取后保存。临时签名跨版本发生变化时仍需迁移；固定签名证书才是保持长期身份连续性的方案。

## 1.2.2 界面调整验证

- 「让语言不再有距离。」移至译文空白区；移除译文空白图标、原有提示及窗口底部标语。
- 原文区底部提供清空和复制原文，译文保留独立复制入口；内容为空时禁用相应操作。
- 主窗口移除置顶入口和快捷键示例；状态栏菜单通过分隔线组织翻译入口、窗口与快捷键、设置与更新。置顶勾选、全局快捷键说明及操作可用性随状态更新。
- 翻译按钮禁用态使用系统 labelColor 和 controlBackgroundColor，保持文字可读，并提供缺少 API / 原文的悬停说明；保留 Command-Return 操作。
- 使用独立 NSHostingView 预览程序渲染浅色/深色空白与结果状态、440pt 最小宽度，共 5 张图并人工查看，无文字裁切。预览使用测试内容，未读取用户 API 密钥或调用网络服务。
- 54 项既有本地回归检查通过；Release 构建与嵌套代码签名校验通过。版本为 1.2.2 / build 5。
- 视图渲染验证不等于真实菜单点击和全局快捷键端到端验证；本次未声称完成这些系统 UI 交互实测。

## 1.3.0 GitHub 更新与统一操作栏

- 原文和译文共用 TextPanelActions 组件；两边都将字数、清空及复制放在底部。原文顶部仅保留标题，移除原剪贴板按钮。
- 译文清空调用现有取消/失效处理，保留原文并阻止在途任务重新写回旧结果。
- 浅色、深色、最小窗口及示例译文的原生渲染检查通过；54 项已有回归检查通过。未调用实际翻译服务。
- 1.3.0 / build 6 Release 构建及嵌套代码签名校验通过；Info.plist 内置固定 GitHub Release appcast 地址。
- 发布脚本 Python 语法检查通过；签名私钥保留本机钥匙串。线上更新签名与真实安装验证将在发布后补充，当前不视为通过。
